kind: Service
apiVersion: v1
metadata:
  name: product-catalog-helidon
  labels:
    app: product-catalog-helidon
spec:
  type: ClusterIP
  selector:
    app: product-catalog-helidon
  ports:
    - name: tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: product-catalog-helidon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-catalog-helidon
  template:
    metadata:
      labels:
        app: product-catalog-helidon
        version: v1
    spec:
      containers:
      - name: product-catalog-helidon
        image: product-catalog-helidon
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
