# Microprofile server properties for testing
server.port=0
server.host=localhost

# Disable metrics for tests
metrics.rest-request.enabled=false

# Test database connection properties (will be overridden by Testcontainers in integration tests)
db.connection.url=jdbc:postgresql://localhost:5432/product_catalog_test
db.connection.username=test_user
db.connection.password=test_pass

# Logging configuration for tests
java.util.logging.level=WARNING
com.thedavestack.productcatalog.level=FINE