# Environment Configuration for Product Catalog
# Copy this file to .env and customize the values for your environment

# Application Configuration
APP_PORT=8080
SPRING_PROFILES_ACTIVE=prod,docker

# Database Configuration
DATABASE_NAME=product_catalog
DATABASE_USERNAME=user
DATABASE_PASSWORD=change-me-in-production

# JWT Configuration (IMPORTANT: Change in production)
JWT_SECRET=change-me-production-secret-key-must-be-at-least-256-bits-long
JWT_EXPIRATION=86400

# MCP Server Configuration
MCP_SERVER_ENABLED=true
MCP_SERVER_NAME=product-catalog-mcp-server
MCP_SERVER_VERSION=2.0.0
MCP_SSE_ENDPOINT=/sse

# Connection Pool Settings
HIKARI_MAX_POOL_SIZE=15
HIKARI_MIN_IDLE=5
HIKARI_IDLE_TIMEOUT=300000
HIKARI_CONNECTION_TIMEOUT=20000
HIKARI_MAX_LIFETIME=1200000
HIKARI_LEAK_DETECTION=60000

# Logging Configuration
LOG_LEVEL=INFO

# Server Configuration
SERVER_PORT=8080
COOKIE_SECURE=true
SESSION_TIMEOUT=30m

# Actuator Configuration
ACTUATOR_ENDPOINTS=health,info,metrics,productmetrics,audit
ACTUATOR_HEALTH_DETAILS=when-authorized

# Async Processing
ASYNC_CORE_POOL_SIZE=2
ASYNC_MAX_POOL_SIZE=5
ASYNC_QUEUE_CAPACITY=100

# Monitoring (Optional)
PROMETHEUS_ENABLED=false
TRACING_SAMPLING=0.0

# Nginx Configuration (if using reverse proxy)
NGINX_PORT=80
NGINX_HTTPS_PORT=443

# Development Only (do not use in production)
# JWT_SECRET=dev-secret-key-1234567890123456789012345678901234567890

# =============================================================================
# MCP Server Features & Integration Guide
# =============================================================================
#
# The Model Context Protocol (MCP) server provides comprehensive AI integration:
#
# 🔧 TOOLS (9+ Available):
# - Product CRUD operations with validation and audit
# - Advanced search and filtering with pagination  
# - Inventory management and low-stock alerts
# - Bulk operations with atomic transactions
# - Business intelligence and analytics
#
# 📊 RESOURCES (20+ Available):
# - product://catalog/all - Complete product catalog
# - inventory://status/overview - Real-time inventory health
# - categories://details/{category} - Category-specific information
# - api://documentation/* - Complete API documentation
# - business://rules/* - Business rules and validation logic
#
# 🎯 PROMPTS (7+ Available):
# - product-creation-guide - Guided product creation
# - inventory-analysis - Stock management recommendations
# - pricing-strategy - Competitive pricing guidance
# - data-quality-audit - Quality improvement workflows
# - troubleshooting-assistant - Issue resolution
#
# 🔗 CLIENT CONNECTION:
# - Endpoint: http://localhost:8080/sse (or https in production)
# - Authentication: Bearer JWT token required
# - Supported roles: ADMIN (full access), USER (read-only)
# - Connection type: Server-Sent Events (SSE)
#
# 📋 EXAMPLE WORKFLOWS:
# 1. Product Creation: Use category details → creation guide → create tool
# 2. Inventory Management: Check status → analyze alerts → update stock
# 3. Business Analysis: Get catalog summary → analyze performance → optimize
# 4. Quality Improvement: Run audit → identify issues → execute cleanup
#
# 🛡️ SECURITY NOTES:
# - All MCP operations require JWT authentication
# - Role-based access control (ADMIN vs USER permissions)
# - Rate limiting configured for MCP endpoints in nginx
# - CORS enabled for cross-origin MCP client access